# Data Lifecycle

User data is created locally, stored in the SQLite database and local attachment area, exported/backed up only after user action, and removed through explicit deletion/reset. Backups are never uploaded automatically. OS uninstall behavior may remove app-private data.
